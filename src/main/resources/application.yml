logging:
  level:
    root: INFO
    application.controller: INFO #Change to DEBUG when required. Add more packages below.
    application.service: INFO #Change to DEBUG when required. Add more packages below.